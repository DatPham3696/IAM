spring:
  application:
    name: security-demo
  datasource:
    url: jdbc:postgresql://localhost:5432/TestUser
    username: postgres
    password: postdb
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format-sql: true
      show-sql: true
  security:
    authentication:
      jwt:
        key_store: keystore.jks
        key_store_password: phamthanhdat
        key_alias: viz
    user:
      name: admin
      password: password
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liSMUAYlfBae7nWd
            client-secret: 4027d992c26ec04f7beb46c932c085d845bfb60e


